using UnityEngine;

public class Skibidi : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hola Teleton't");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
